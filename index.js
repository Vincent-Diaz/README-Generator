const fs = require("fs");
const inquirer = require("inquirer");
const markDown = generateMarkDown();
// array of questions for user
const questions = [ "What is the title of this project?", "What is the desription of this project?", "What are the installation instructions?", "What will this project be used for?", "Who contributed to this project?", "What are the test instructions?"
];

// function to write README file
function writeToFile(fileName, data) {
}

// function to initialize program
function init() {

}

// function call to initialize program
init();
